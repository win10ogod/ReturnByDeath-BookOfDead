modLoader="javafml"
loaderVersion="${loader_version_range}"
license="${mod_license}"
[[mods]]
modId="${mod_id}"
version="${mod_version}"
displayName="${mod_name}"
description='''
死亡回歸與死者之書：世界回歸、生命記憶、見知與書庫。
Whole-save returns, subjective life recordings, identity-gated memory books, and an in-world archive.
Minecraft 1.21.1. See the included operations and validation documents for recording fidelity and supported integrations.
'''
[[dependencies.${mod_id}]]
modId="neoforge"
type="required"
versionRange="[${neo_version},)"
ordering="NONE"
side="BOTH"
[[dependencies.${mod_id}]]
modId="minecraft"
type="required"
versionRange="${minecraft_version_range}"
ordering="NONE"
side="BOTH"

[[mixins]]
config="rbd.mixins.json"
